#!/usr/bin/env macruby

command = ARGV.shift

case command
when "new"
  require 'rucola/generators/rucola/app/app_generator'
  
  # Rucola::Generators::App::AppGenerator.start
  Rucola::Generators::App::Type.start
else
  puts "Que?? #{command} #{ARGV.join(' ')}"
end
