MacRuby port:

Cleanup:
- Check if we really need the Rucola::RCApp controller/view methods
- Check if we still need Rucola::RCController etc
- Check if we really need the File core_ext.
- Credit Rails for boot code and Merb for some String methods.